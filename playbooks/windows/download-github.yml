---
- hosts: workstations
  tasks:

  #Installation of google earth through the github repository
  #Learning how to install applications we store on the repository
    - name: Install google earth from github repository
      win_package:
        path: https://github.com/fped4l/iac-ansible/raw/main/applications/mysql-workbench-community-8.0.26-winx64.msi
        state: present
        provider: auto

    - name: Install google earth from github repository2
      win_package:
        path: https://github.com/fped4l/iac-ansible/raw/main/applications/VSCodeUserSetup-x64-1.57.1.exe
        state: present
        product_id: auto

  